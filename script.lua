local http = require('socket.http')
local ltn12 = require('ltn12')
local q = io.read()


wait(0.2)
file:read("./output.txt")
io.write("what title dumbass: ")

function test(url)
    url = "https://api.stemplayer.com/remixes"
    url2 = "https://api.stemplayer.com/"
    text = "remixes"
    
  
    for _,v in next, url do 
        http.get('https://api.stemplayer.com/remixes')
        if socket.http.response = 200 then
            os.execute('cls')
            print("stemplayer is online. please wait.")
    else if socket.http.response = 404 then 
        http.get('https://api.stemplayer.com'...text...math.rand(3,200))
        headers.append(url2)
        end
    end
end

test(url)
until true then 


local headers = {
    'Content-Type': 'application/json'
},

for i,v in ipairs(headers) do 
    http.post(headers, table.sort(headers, headers.__newindex(headers, i, v, [q])))))])
    return v
end
